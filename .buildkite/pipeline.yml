name: "Qwiet preZero Scan"
description: "Pipeline that will perform Qwiet Scanning"
emoji: ":rocket:"
tags: ["appsec"]
steps:
  - label: ":white_check_mark: Test"
    command: |
      curl https://cdn.shiftleft.io/download/sl > ./sl && chmod a+rx ./sl
      ./sl analyze --strict --wait --app myApp  .
      ./sl check-analysis --v2 --app myApp --branch=master --target tag.branch=releasebranch --config shiftleft.yml
    key: test
